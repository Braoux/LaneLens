# Ne jamais préfixer les secrets par VITE_ (exposés au navigateur).
# Configuration du provider OpenAI, exclusivement côté serveur.
# OPENAI_API_KEY est requise uniquement lors de l'instanciation du provider.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-6-sol
OPENAI_TIMEOUT_MS=30000
